import { Box, Typography } from "@mui/material"

const Home: React.FC = () => {
    return (
        <Box>
            <Typography variant="h1">Thios page is home</Typography>
        </Box>

    )
}

export default Home